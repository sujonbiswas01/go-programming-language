package main

func main() {
	var numbers [6]int
	numbers[1] = 4
	numbers[0] = 40

	numbers[5] = 100
	// fmt.Println("length number is ", len(numbers))
	// fmt.Println(numbers[0])

	// for i := 0; i < len(numbers); i++ {
	// 	fmt.Println(numbers[i])
	// }
	slice()
}
